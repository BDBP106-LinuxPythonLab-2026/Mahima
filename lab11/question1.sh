#(i)The stduot of the command went to the file presentfiles as before the > "1" file descriptor was given. Whereas  the error was saved in the file filesnotpresent as "2" file descriptor was given before the redirection.
#(ii)The stdout of the command was redirected while the error was thrown on the screen as ">" by default only redirects the stdout of the command to the file.

